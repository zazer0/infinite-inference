[project]
name = "infinite-inference"
version = "0.1.0"
description = "parallelising multi-model GPU inference!"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "setuptools>=80.9.0",
    "sglang>=0.4.6.post5",
    "vllm>=v0.8.5.post1",
]
